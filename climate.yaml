  - platform: generic_thermostat
    name: garage
    heater: switch.heatinggarage
    target_sensor: sensor.garagetemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 16
    precision: 0.1

  - platform: generic_thermostat
    name: livingroomkitchen
    heater: switch.heatinglrkitchen
    target_sensor: sensor.livingroomkitchentemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 16
    precision: 0.1

  - platform: generic_thermostat
    name: livingroomwindows
    heater: switch.heatinglrwindows
    target_sensor: sensor.livingroomwindowstemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 16
    precision: 0.1