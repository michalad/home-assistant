#############
## A
#############
  
  - platform: generic_thermostat
    name: boilerroom
    heater: switch.heatingboilerroom
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 20
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: garage
    heater: switch.heatinggarage
    target_sensor: sensor.garagetemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 20
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: office
    heater: switch.heatingoffice
    target_sensor: sensor.officetemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 20
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: laundry
    heater: switch.heatinglaundry
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 20
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: bathroomground
    heater: switch.heatingbathroomground
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 22
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: hallground
    heater: switch.heatinghallground
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1


#############
## B
#############

  - platform: generic_thermostat
    name: kitchen
    heater: switch.heatingkitchen
    target_sensor: sensor.wifi_temperature_humidity_sensor_temperature
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: lrwindows
    heater: switch.heatinglrwindows
    target_sensor: sensor.livingroomwindowstemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: lrkitchen
    heater: switch.heatinglrkitchen
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

#############
## C
#############

  # Zuzia
  - platform: generic_thermostat
    name: room1_suzanne
    heater: switch.heatingroom1_suzanne
    target_sensor: sensor.room1_suzanne
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 22
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: room2
    heater: switch.heatingroom2
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  # Sypialnia
  - platform: generic_thermostat
    name: room3_bedroom
    heater: switch.heatingroom3_bedroom
    target_sensor: sensor.room3_bedroomtemp
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: room4
    heater: switch.heatingroom4
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: mezzanine
    heater: switch.heatingmezzanine
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: hallfloor
    heater: switch.heatinghallfloor
    target_sensor: sensor.TODO
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 21
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1

  - platform: generic_thermostat
    name: bathroomfloor
    heater: switch.heatingbathroomfloor
    target_sensor: sensor.floor_bathroom
    min_temp: 15
    max_temp: 30
    ac_mode: false
    target_temp: 22
    cold_tolerance: 0.5
    hot_tolerance: 0.5
    min_cycle_duration:
      seconds: 5
    keep_alive:
      minutes: 3
    initial_hvac_mode: "heat"
    away_temp: 18
    precision: 0.1